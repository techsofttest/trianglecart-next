import ProductsPageClient from '@/components/product/ProductsPageClient';

export const metadata = {
    title: 'Spices - Red Chilli | Triangle Cart',
    description: 'Browse all spices, red chilli, and items on Triangle Cart.',
};

export default function RedChilliRedirectPage() {
    return <ProductsPageClient />;
}
